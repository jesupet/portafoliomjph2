import { createStore } from 'vuex'
import proyectos from './proyectos.json'

export default createStore({
  state: {
    proyectos: proyectos,
  },
  getters: {
  },
  mutations: {
  },
  actions: {
    },
  modules: {
  }
})
